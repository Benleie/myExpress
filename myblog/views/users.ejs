<%- include('header') %>

<div class="ui">
    <p>
      <span class="tag"><%= user.name %></span>注册于<%= user.created_at %>
      <span>访问次数(<%= user.pv %>)</span>
    </p>
</div>

<% posts.forEach(function (post) { %>
  <%- include('components/posts-content', { post: post }) %>
<% }) %>

<%- include('footer') %>